"""Contradiction analyst — detects epistemic stalemates and conflicts."""


def run(evidence: dict) -> list[dict]:
    """Scan evidence for contradictions. Returns list of contradiction dicts."""
    return []
