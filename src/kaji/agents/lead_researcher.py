"""Lead researcher agent — claim decomposition and broad evidence gathering."""


def run(claim: str, domain: str) -> dict:
    """Execute lead researcher tasks. Returns collected evidence."""
    return {"status": "stub", "claim": claim, "domain": domain}
